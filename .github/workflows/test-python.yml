name: test-python
env:
  TZ: Asia/Shanghai

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: install python
        run: |
          sudo apt-get update
          sudo apt-get install -y python2-minimal

      - name: check python
        run: |
          python2 --version
          which python2
